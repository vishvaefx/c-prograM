#include<stdio.h>
int main()
{
 int a;
 printf ("enter a number");
 scanf("%d",&a);
 if (a>0)
 printf("given value is +ve");
 if (a<0)
 printf("given value is -ve");
 if (a==0)
 printf("given value is 0"); 
 return 0;
 
}